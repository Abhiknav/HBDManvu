Drop real photos here (1.jpg through 10.jpg) matching the `photo` paths in
BALLOONS.notes inside src/app/core/content.config.ts. Each one becomes the
polaroid that flies out when that balloon is popped. Until added, a soft
placeholder frame is shown instead.
