Drop real photos here (1.jpg through 6.jpg) matching the `photo` paths in
STORY inside src/app/core/content.config.ts. Until then, each chapter shows
an elegant placeholder frame instead of a broken image.
